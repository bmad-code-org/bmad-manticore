"""mc-prompter server package (Phase A).

Launched as `python -m server.main` with cwd set to the scripts directory,
so the package resolves identically under both launch paths (uv run with
aiohttp as a PEP 723 dependency now; the prompter-lab workspace venv in
Phase B).
"""
